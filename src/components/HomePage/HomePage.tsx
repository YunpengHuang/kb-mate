import { KeyboardPartial } from "@/src/atoms/snippetAtom";
import React from "react";

type HomePageProps = {
  keyboardData: KeyboardPartial;
};

const HomePage: React.FC<HomePageProps> = ({ keyboardData }) => {
  return <></>
};
export default HomePage;
